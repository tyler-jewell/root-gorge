name: marker_repository
description: A Flutter marker repository that uses Firebase Firestore storage.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  cloud_firestore: ^3.1.10
  firebase_core: ^1.13.1
  flutter:
    sdk: flutter
  geoflutterfire: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0
